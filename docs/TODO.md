#Pending

- [ ] Backup strategy (§17) — scheduled `pg_dump` + Chrome profile + `.env` + workspaces off-box (tailnet NAS or encrypted bucket), reusing the updater's process pattern. Before anything irreplaceable lands.
- [ ] Agent generated code running in the context of the tools. — capability authoring: let Alfred write reusable scripts/skills (over the Python sandbox + workspace) that call other tools and can be re-invoked by name; the seed of self-extension.
- [ ] Discord bot (build-order step 6) — second ingress over the same conversations/runs, proving "one Alfred, many surfaces, shared memory"; reactions as the approval UI.
- [ ] Long-term memory phase 2 (pgvector semantic recall, §6.4) — the deferred half of the memory increment: add an `EmbeddingProvider` sibling in agent-core, `CREATE EXTENSION vector` + an `embedding` column on `memory_facts` (one migration, dimension chosen with the model then), embed on `remember` (cost via `recordOutOfLoopLlmCall`), and swap the body of `readMemoryFacts` from "all global facts" to top-K by embedding distance — the single seam, nothing else moving. Gated on the fact set outgrowing the prompt.
- [ ] Identity block + persona file (§7.5) — inject the `[system]` identity block (current time, ingress, user) the loop currently omits, and move the hardcoded `SYSTEM_PROMPT` to `packages/agent-core/personas/alfred.md`. Small, improves every turn, and a prerequisite for autonomous runs (which need time + presence).
- [ ] Per-run cost cap enforcement (§10.7) — the planned-but-unbuilt budget guard: a configurable per-run USD cap checked after each LLM call, failing the run with `error='cost_exceeded'`. Cost is already computed onto `agent_runs.cost_usd`; nothing enforces it. A safety net before autonomous / looping runs.

## Brainstorm panel — 2026-06-16 (Claude · Gemini · Codex, blind cross-rated 1–5)

Ranked by mean panel score. The top cluster is a coherent build sequence: autonomy plus the safety rails that make it survivable.

- [ ] **Semantic Memory With Confidence** (Codex · avg 5.00) — pgvector recall with fact salience, source-conversation links, and confidence/recency scoring, so recall stays relevant and bounded as memory grows. *Extends the existing "Long-term memory phase 2" item above with the scoring/salience layer.*
- [x] **Autonomous Watchers** (Codex · avg 5.00) — ✅ BUILT (CHANGELOG 86–87) — cron, inbox, webhook, and self-scheduled triggers that create normal agent jobs with clear provenance and approval boundaries; turns Alfred into a quiet background operator. The general capability the "Morning-briefing trigger" item above is the first concrete slice of (§9.4/§7.7).
- [ ] **Task-Scoped Browser Vaults** (Codex · avg 5.00) — wrap browser automation in disposable task sessions with domain allowlists, profile/container selection, and per-task approval gates (purchases/messages/account changes); the structural browser containment §16 flags as not-yet-built, preserving real-login power while making risky browser work contained and auditable.
- [ ] **Reversible Actions / Action Receipts** (Claude · avg 4.67) — every write/destructive tool emits a structured receipt carrying an inverse where one exists (unsend→draft, file restore from a pre-write workspace snapshot, move-back), adding "recover after" on top of "ask before"; fits the `tool_calls` audit.
- [ ] **Cost & Risk Governor** (Codex · avg 4.67) — per-run and daily cost caps, tool-risk budgets, escalation rules, and "continue?" checkpoints when a run gets expensive/long/unusually risky; extends the per-run cost-cap item above toward the §7.7 layered run/objective/daily budgets.
- [ ] **Teach Alfred a Skill** (Claude · avg 4.33) — promote a working Python snippet or a recorded browser sequence into a named, reusable tool registered in the catalog with its own trust tier; makes "grow over time" self-serve. Overlaps "Agent generated code running in the context of the tools" above.
- [ ] **Push Notifications + Deep-Linked Approvals** (Claude · avg 4.33) — web Push + iOS APNs so a backgrounded approval/question or a finished autonomous run pings the owner, tapping straight to the approval card; the connective tissue that makes autonomy + "fail loudly" reach a human who isn't watching the chat.
- [ ] **Personal Knowledge Base (RAG)** (Claude · avg 4.00) — drop PDFs/notes/web-clips into a workspace-backed, pgvector-indexed corpus Alfred can search and cite; turns Alfred into a librarian of the owner's own documents, reusing the phase-2 pgvector work.
- [ ] **Personal Ops Dashboard** (Codex · avg 4.00) — a compact owner console showing active runs, queued jobs, waiting approvals, recent triggers, browser-bridge health, cost burn, deploy version, and failed jobs; extends `/debug` from a ledger into an operator view instead of debugging through chat and logs.
- [ ] **Steerable Plan Mode** (Claude · avg 4.00) — for multi-step tasks Alfred maintains a visible, live-updating checklist the owner can edit or redirect mid-run; realizes the reserved §7.7 objective-scratchpad seam and makes long agent loops transparent and correctable.
- [ ] **Tool Workflow Recipes** (Gemini · avg 3.67) — define and save named sequences of tool actions (e.g. summarize latest emails then draft a reply), invokable by name with a single upfront approval covering the trusted sequence.
- [ ] **Feedback-Driven Refinement Loop** (Gemini · avg 3.67) — when the owner cancels a run or gives negative feedback, capture the context + correction to refine future decisions (new memory facts, tool-selection priorities). *Panel flagged overfitting-to-sparse-corrections risk.*
- [ ] **Ambient Memory Digest** (Gemini · avg 3.00) — proactively analyze new information (emails, browsing, chat logs) and periodically generate concise personalized digests from long-term memory, presented non-intrusively. Depends on watchers + notifications existing first.
- [ ] **Contextual Persona Adjustment** (Gemini · avg 2.67) — adapt communication style, proactive suggestions, and tool availability to device/location/a declared context ("work mode"/"personal mode"). *Panel: nice polish but can feel unpredictable; less important than reliability/memory/control.*
- [ ] **Personal Data Insights Dashboard** (Gemini · avg 2.00) — privacy-preserving analytical insights from aggregated owner data (browsing history, email metadata, file changes) in a web dashboard. *Lowest-rated: quantified-self analytics, peripheral to "an agent that acts."*

# Done

- [x] Autonomous watchers / triggers (build-order step 9) — the `alfred-triggers` scheduler + the tiered detection ladder (read-tier `tool.invoke()` gate → cheap-model triage → action run) + a durable notifications outbox + Web Push, with the `schedule_self` tool and watcher scratchpad continuity (`memory_facts` `scope='trigger:<id>'`). Generalizes + subsumes the "Morning-briefing trigger" vertical slice. (CHANGELOG 86–87)
- [x] Long-term memory — plain-rows foundation (the "one Alfred, one memory" pillar): `memory_facts` + migration 0009, the agent-called `memory` tool family (`remember`/`forget`/`list_memories`, group 'memory') as the write path, and automatic recall (`readMemoryFacts` folded into the system prompt) as the read path; provenance via `source_run_id`, extraction resolved as agent-tool-flagged. pgvector semantic recall deferred to phase 2 (now its own Pending item). (CHANGELOG 84)
- [x] Show token and cost at the bottom of a conversation, update along the way — persistent footer (web + iOS) showing cumulative tokens + USD cost, climbing live via a new `usage` SSE event overlaid on the `GET /api/conversations/:id` baseline. (CHANGELOG 80)
- [x] Prevent lock screen, because it stops the audio and mic — iOS VoiceController disables the system idle timer (`UIApplication.shared.isIdleTimerDisabled`) for the whole hands-free session (set on successful start, cleared in stop()). (CHANGELOG 79)
- [x] In the iOS app, I would like list of "servers" and an option for testing in the configuration. — Settings now holds a list of saved servers (label + URL), tap-to-activate, swipe-delete, and a per-row connection test (GET /api/health). (CHANGELOG 81)
- [x] In the iOS app, I would like to see the SW version in the configuration page somewhere. — Settings "About" shows AppInfo.version (CFBundleShortVersionString + build), plus the active server's last-tested version. (CHANGELOG 81)
- [x] Auto update harnes
- [x] Debug page should be per conversation — reworked into a Conversation → Run → LLM-call/tool ledger (rail with run-status sparkline + token/cost totals, lazy-loaded per-run detail). (CHANGELOG 49)
- [x] Wire up `ask_user` / the question pause path — built-in tool creating a `kind='question'` interaction, reusing the existing approval machinery (§7.3/§10.2). Lets Alfred ask the owner structured questions mid-run. (CHANGELOG 59; question-card "Other" free-text fix CHANGELOG 60)
- [x] Conversation list / history view in the web client — URL routing `/conversation/:id` + a collapsible history sidebar backed by `GET /api/conversations`, ordered by the resurrected `last_active_at`. (CHANGELOG 63)
- [x] LLM retry/backoff (§10.7) — agent-core `RetryProvider` decorator wrapping `TracingProvider` (each failed attempt its own `llm_calls` row): 4 retries at 1/2/4/8s on `TransientLlmError` before the first streamed event, exhausted → `llm_unavailable:`-prefixed run failure; aborts never retried. (CHANGELOG 66)
- [x] User-initiated cancellation (§10.6) — route-authoritative `POST /api/conversations/:id/cancel` (terminal write + §10.9 cascade via the shared `terminateRuns`) + worker `AbortController` killing the in-flight LLM stream + web Stop button with refresh-proof busy. (CHANGELOG 65)
- [x] Python execution sandbox — run_python + pip_install over a shared lazily-created venv, cwd = the conversation workspace, approval-gated as group 'python'. (CHANGELOG 57)
- [x] Persist approval accross restarts — "Don't ask again" on the approval card persists into tools.require_approval (same store as the Tools page); + sweep bugfix for zombie awaiting_approval runs. (CHANGELOG 43)
- [x] Email tools — built-in IMAP/SMTP family (list/search/read + save_draft/send_email), provider-agnostic, connect-per-call, group 'email' (reads read-tier, send/draft write-tier). (CHANGELOG 61)
- [x] Tool usage is hidden while its happening in web app, I think we should show this
- [x] If no internet and "fetch failed", give better error message
