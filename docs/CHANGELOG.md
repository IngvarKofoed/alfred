# Changelog

Each entry is numbered with a monotonically increasing integer. Append new entries to the end. Never reuse or reorder numbers. Numbers are globally unique across this file and `docs/CHANGELOG-archive.md` — never reused.

> **Entries 1–95 are archived in [`docs/CHANGELOG-archive.md`](./CHANGELOG-archive.md)** (MVP build + early post-MVP). Milestone summary:
>
> **MVP (build-order steps 1–6) — complete.** Hono webserver + Vite/React PWA over Tailscale (1) → Postgres + Drizzle schema (2) → agent-core: hand-rolled loop, `LlmProvider` abstraction + Gemini, `Tool` interface (3) → real model end-to-end via `alfred-worker` (pg-boss → NOTIFY → SSE → web), in-Postgres observability (`llm_calls` + `/debug`), and tools + trust-tier approval (4) → embedded Chrome-extension browser bridge + vision/image support + per-conversation workspaces (5) → Discord bot, the second interactive ingress (6).
>
> **Early post-MVP, built interleaved through entry 95:** Tools page + persisted per-tool/group approval; chat slash-commands + autocomplete; the `ask_user` question pause path; Python execution sandbox; email tools (IMAP/SMTP); conversation-history sidebar + auto-naming; user-initiated cancellation; LLM retry/backoff; native iOS text-chat app + hands-free voice (server-side STT/TTS) + voice cost accounting; multi-model image generation + out-of-loop AI-tool cost accounting; auto-deploy updater + git-describe versioning; long-term memory (plain-rows `memory_facts` + automatic recall); autonomous watchers/triggers (`alfred-triggers` + the tiered detection ladder + Web-Push outbox).

96. Included `alfred-discord` in the root `pnpm dev` concurrently set (it was deliberately left out at build time, CHANGELOG 95) — `dev` now runs worker/server/web/**discord** together. Safe in the `-k` (kill-others) set because the bot idles (never exits) when unconfigured; and hardened the boot path so a `client.login` failure — a bad/expired token, or (the likely first-run case) the privileged Message Content Intent not yet enabled in the developer portal — is caught, logged loudly, and **idles** rather than exiting, so a Discord login failure can't crash-loop the process under pm2 or take worker/server/web down with it under `concurrently -k`. `pnpm --filter @alfred/discord build` + `pnpm lint` green.

97. Discord: the bot's reply no longer pings the owner. The placeholder was sent via `message.reply('…')`, which Discord renders as a reply that **mentions** the author (the "@you" ping) — pure noise in a 1:1 DM. Now `message.reply({ content:'…', allowedMentions:{ repliedUser:false } })` (kept `message.reply` over `channel.send` because `message.channel`'s union includes `PartialGroupDMChannel`, which has no `.send`). `pnpm --filter @alfred/discord build` + `pnpm lint` green.

98. Wrote docs/specs/2026-06-17-discord-conversation-model.md — spec for the Discord conversation model (a brainstorm-with-codex+gemini → spec). Moves Discord from "one infinite DM = one ever-growing conversation" to a **private Alfred guild where a forum post is a conversation**: a forum post is a thread with its own channel id and the bot already keys conversations on `message.channelId`, so post-per-conversation drops onto the existing `(ingress, channel_key)` model with **zero schema change** (`channel_key = post/thread id`). The DM is **retired** — it just redirects to the guild, creating no conversation. Each recurring watcher becomes its own forum post too, which **collapses the notification problem**: a watcher posts into its thread, Discord's native push *is* the alert, and approvals are the same in-thread buttons — no separate notifier, no leaving Discord (Web Push stays for the web PWA, untouched). The load-bearing seam is the watcher→thread move: `run.ts` resolves the watcher's scratchpad by `conversation_id` (a new `getTriggerByConversation`) instead of `getTrigger(conv.channelKey)`, `detect.ts` routes each escalation through `triggers.conversation_id`, and a **bot-side watcher-reconcile** creates the posts (setting `conversation_id`) and proactively `LISTEN`s their channels up front (a watcher run has no preceding owner message). All gated on a new optional `DISCORD_GUILD_ID` — unset ⇒ today's DM-or-mention bot unchanged, so the change is additive. Settled the alternatives via the picker: sessionized DM (rejected — a "fake sidebar"), a doorbell `DiscordNotifier` (rejected — owner won't use the web app), and a read-only mirror seam (rejected — a reply wouldn't continue the watcher). Read-only spec phase — no code changed.

99. Built the Discord conversation model — guild forum threads (spec `docs/specs/2026-06-17-discord-conversation-model.md`; via a multi-agent workflow → adversarial 3-lens review → fix). Discord becomes a self-sufficient surface for a Discord-only owner: a private Alfred guild where a **forum post is a conversation** and each recurring watcher is its own post. **config:** new optional `DISCORD_GUILD_ID` — unset ⇒ today's DM-or-mention bot is byte-for-byte unchanged; set ⇒ the bot uses the forum-thread conversation model. **db:** `getTriggerByConversation(db, conversationId)` (the recurring-watcher lookup keyed on `triggers.conversation_id`, deterministic) + `setTriggerConversation(db, { id, conversationId })` (the bot's repoint). **worker (`detect.ts`):** `resolveConversationId` now routes a recurring watcher's escalation through `triggers.conversation_id` **when set** (the Discord post the bot created), else today's `trigger.id`/`ingress='trigger'` default — with a **NULL-guarded backfill** so the bot's repoint wins (the backfill only fills a still-NULL row, never overwrites the Discord conversation). Backward-compatible: an existing watcher with no `conversation_id` is unchanged and keeps its scratchpad (`memory_facts` scope `trigger:<id>`, keyed on the trigger id, so it survives the repoint). **worker (`run.ts`):** watcher resolution swapped from `getTrigger(conv.channelKey)` to `getTriggerByConversation(db, conv.id)`, keeping the `isTrigger` gate and a **recurring-only alias** so `'self'` one-shots and interactive runs are byte-for-byte unchanged (`human_in_loop=false` keeps `isTrigger` true regardless of ingress, so the §7.7 autonomous lifecycle is intact). **services/discord (the guild forum model, all gated on `DISCORD_GUILD_ID`):** find/create the `conversations` + `watchers` GuildForum channels; **mention-less** responses inside the Alfred guild (answer every owner message in a forum post, no `@` needed) + a one-line **DM redirect** (a DM creates no conversation/run); a bot-local **`/new [title]`** slash command (create a post + greeting); a **catch-all** conversation for stray in-guild messages outside a post; a **watcher-reconcile** (boot + 30s timer) that creates a `watchers` post per enabled watcher, repoints its `conversation_id`, and opens a standing `LISTEN` so the streamed run renders into the post; and **lazy watcher-render** — a watcher run has no user-message placeholder, so the bot posts a target message into the watcher's post on the run's first streamed event. Also a `standingListens` fix so an owner reply *in* a watcher post doesn't tear down the standing subscription. **Notifications become Discord-native** for the Discord-primary owner — a watcher posting into its thread is the alert; the Web Push outbox/dispatcher are off the critical path but untouched (still serve the web PWA). Post-review (adversarial 3-lens — blocker + should-fix + nit findings applied): the ungated `/self`-reminder notify-policy regression → keep `watcher` undefined for `'self'`; the detect backfill NULL-guard; harden `openWatcherTarget` to confirm a genuine watcher post in the Alfred guild; auto-archive hardening; a tag-required-forum log; and the deterministic `getTriggerByConversation`. **No schema change, no migration.** Verified: build/lint green; tests 157 passed / 1 pre-existing local-DB-state flake (the `run.test.ts` approval-gate, CHANGELOG 33/39/57/59/61/65/84) + 18 Discord helper tests. Live Discord behaviour (a real guild with Community enabled for forums + the Manage Channels permission) pending on-box verification.

100. Discord conversation-model refinements from live use (four owner-requested tweaks). (1) The bot now **writes plain messages instead of replying** — the placeholder is `message.channel.send('…')` (guarded by `isSendable()`, which narrows out the `.send`-less `PartialGroupDMChannel` that originally forced `message.reply`) rather than a reply-with-reference; in a 1:1 post the "replying to…" reference was pure noise (supersedes the CHANGELOG-97 ping-suppression). (2) **Tool usage is now visible in Discord** — each `tool_call_start` is woven into the streamed message as a quiet Discord-subtext chip (`-# 🔧 \`tool\` <short args>`, args truncated to ≤80 chars and only when the worker included them), mirroring the web client's inline chips. (3) **The forum post's title is the conversation title** — `getOrCreateConversationByChannel` gained an insert-only `title` (set on creation, never clobbering a later rename/auto-title); `onMessageCreate` passes the post's name for a thread, and the watcher-reconcile passes the watcher name, so a post-backed conversation is named by its post and the worker's auto-title is skipped. (4) **Discord conversations stay in the web history list but lose the "discord" badge** — by owner request the conversations remain visible (the all-ingress CHANGELOG-92 behavior is kept; `GET /api/conversations` is unchanged), but the web `Sidebar.tsx` no longer renders a `discord` tag on them (the `'discord'` case was dropped from `badgeLabel`; `trigger`→"watcher" and `voice`→"voice" tags stay). Touches `packages/db/src/queries.ts`, `services/discord/src/index.ts`, `clients/web/src/Sidebar.tsx`. `pnpm -r build` + `pnpm lint` green; no schema change.

101. Auto-deploy: the updater now redeploys `alfred-triggers` and `alfred-discord`. The `DEPLOY_APPS` default was still `alfred-webserver,alfred-worker`, but the updater only stops/(re)starts the apps in that list (`pm2 start ecosystem.config.cjs --only <DEPLOY_APPS>`) — so although a deploy *built* the new code (`pnpm -r build`), it never (re)started the triggers scheduler (added to `ecosystem.config.cjs` in CHANGELOG 86) or the Discord bot (CHANGELOG 95): they ran stale code, or never started on the deploy box at all. Bumped the default to `alfred-webserver,alfred-worker,alfred-triggers,alfred-discord` (the ecosystem's full long-running set; `alfred-updater` is still auto-filtered so it never restarts itself; `--only` also starts an app pm2 doesn't yet know, so this self-heals on the next deploy). Also added the missing `DISCORD_GUILD_ID` to DEPLOYMENT.md §13.1's built-keys list. `packages/shared/src/config.ts` default + `.env.example` + DEPLOYMENT.md §13.1; `pnpm --filter @alfred/shared build` + `pnpm lint` green.

102. iOS voice: faster time-to-first-word — dropped the on-device endpointing window `trailingSilenceToEnd` from **1.2s → 0.5s** (`clients/ios/.../VoiceController.swift`). That window is pure local dead time paid every turn: after the owner stops speaking, the app waits that long for silence before it endpoints and begins the STT upload, so it sits in front of the entire serial reply chain (upload → STT → run → LLM-to-first-sentence → first TTS clip). 0.5s is deliberately aggressive for snappiness; the comment notes to raise it if it endpoints on mid-sentence pauses (calibrate against the existing mic-level meter). No change to the cloud legs. Separately confirmed — **no code needed** — that **ElevenLabs flash TTS is already fully built and is a `.env` flip**: `ElevenLabsTtsProvider` already uses the low-latency `eleven_flash_v2_5` model with a default voice (`speech-provider.ts`), `makeTtsProvider()` selects it on `TTS_PROVIDER=elevenlabs`, the config schema has `ELEVENLABS_API_KEY`/`TTS_VOICE`, and iOS already decodes its MP3 (CHANGELOG 73/74); activating it is `TTS_PROVIDER=elevenlabs` + `ELEVENLABS_API_KEY` (+ optional ElevenLabs `TTS_VOICE` voice id) in `.env`, currently unset (Google defaults). `xcodebuild` (generic iOS Simulator) BUILD SUCCEEDED. Live on-device calibration of the 0.5s window pending.

103. The `/speak` read-out command — speak the last assistant reply on demand, web (spec `docs/specs/2026-06-18-read-out-command.md`; built via a multi-agent /workflows run — additive foundation → 3 parallel disjoint consumers → verify — then a code-review high pass whose 3 should-fix + 6 nits were all applied). A web slash command (`/speak`, alias `/read`) takes the conversation's **last assistant message**, sentence-chunks it, synthesizes each chunk to a TTS clip in Alfred's configured voice, and plays it in the browser — **no agent run, no pg-boss job, no LLM loop**: a deliberately run-free, request/response path parallel to the run-coupled iOS voice pipeline (which is untouched). **Shared, ingress-agnostic core** so a future client (Discord/WhatsApp) reuses it by just delivering the clip: `writeBytesToWorkspace` + a new `writeAudioToWorkspace`/`AudioRef` and a `textFromContent` content-text joiner live in `@alfred/shared`; `stripMarkdownForSpeech`, `splitIntoSpeechChunks`, `synthesizeSpeech` (bytes-only, returns null on empty-after-strip) and `synthesizeToClip` (= synth + shared audio writer) in `@alfred/agent-core/src/speech.ts`; `readLastAssistantText` (newest assistant text, via the shared joiner) + `latestRunIdForConversation` in `@alfred/db`. **Web flow:** the `speak` command (pure `@alfred/db`-only registry) returns a new `CommandResult.action: 'speak'` directive; the client opens `POST /api/conversations/:id/speak`, which streams NDJSON `{seq,path,mimeType}` clip refs (Hono `streamText`) as each chunk synthesizes, and plays `/media` clips through an ordered `HTMLAudioElement` queue (the web client's first audio playback; epoch-guarded so a fresh `/speak` or a conversation switch stops the prior one). **Cost** is attributed to the conversation's most recent run via `recordOutOfLoopLlmCall` (an aggregated `'tts'` row, `/debug`-visible; an owner-chosen tradeoff — it doesn't re-sum into that finished run's rolled-up `cost_usd`). **Moves:** `writeAudioToWorkspace` relocated from `services/worker/src/audio.ts` (deleted) to `@alfred/shared`; the worker's `textOf` now delegates to `textFromContent`; both `stripMarkdownForSpeech` and `TTS_MIN_SENTENCE_CHARS` are imported from `@alfred/agent-core` (worker's live incremental flush behaviourally unchanged). **Code-review fixes:** the route now aborts synthesis on `c.req.raw.signal` (no billed TTS for a superseded/abandoned read-out); the `speak` command no longer pre-reads the message (the route's 422 is the single authority, so the message is loaded once — the web client shows a neutral "Nothing to read out yet." on 422 and "🔊 Speaking…" only once the stream is live); the error note is honest ("Some of that…" only if a clip already played); `splitIntoSpeechChunks` is a single forward pass (no per-iteration regex realloc / O(n²) re-slice); empty-after-strip chunks are skipped; and the web play queue removes clip listeners on advance + nulls the audio ref on drain. Web-only; Discord/iOS are deferred fast-follows on the same helpers. `pnpm -r build` (10 projects) + `pnpm lint` green; `pnpm test` 158 passed / 1 failed (the documented local-DB-state `run.test.ts` approval-gate flake, CHANGELOG 33/39/57/59/61/65/84), incl. a new `packages/db/src/db.test.ts` read-out-helpers round-trip. Live in-browser confirm of playback pending (per `clients/web/CLAUDE.md`).

104. Docs: clarified the `DISCORD_GUILD_ID` comment in `.env.example` — Discord "guild" = server, and how to get the value (right-click the server icon → Copy Server ID, with Developer Mode on). No code change.

105. `.env.example` → `.env` sync script (`scripts/sync-env.mjs`, run via `pnpm env:sync`). Cross-platform, pure-Node (no deps): rewrites `.env` to mirror `.env.example`'s line order, comments, and section headers while preserving the user's existing values — keys **added** to the example are added (carrying the example's placeholder + inline docs), keys **removed** from the example are removed only after per-key confirmation. Safety: secrets are never printed (only key NAMES appear in prompts/summaries), `.env` is copied to `.env.bak` before any write, an unchanged rebuild skips the write entirely, and a stale key is never deleted without an explicit `y` (an EOF/piped/non-interactive `AbortController`-aborted prompt defaults to keep). Stale keys the user keeps are appended under a `# === Not in .env.example (kept by sync-env) ===` header. Flags: `--dry-run` (plan only), `--yes` (delete all stale non-interactively), `--no-delete` (keep all stale), `--example`/`--env` path overrides, `--help`; first run with no `.env` just copies the example. `eslint.config.js` adds `AbortController` to the `scripts/**/*.mjs` globals. Verified with temp fixtures (add/delete/keep, order + value preservation, idempotency, first-run, `--yes`/`--no-delete`, CRLF preservation) and a `--dry-run` against the real repo files (15 example keys missing from the local `.env`, no stale); eslint clean. Code-review (high) applied: the output line-ending is taken from the target `.env` (not the example) so the no-op guard holds when the two files differ in LF/CRLF style (no needless rewrite), and the header documents the two by-design limitations — `.env`-only comments/blanks are replaced by the example's layout (values preserved; recover via `.env.bak`) and values are assumed single-line.

106. Audited the CLAUDE.md scaffolding against current scaffold conventions; the five subtree files were already current, the root had drifted on two conventions.
     Root code-review mandate is now `code-review high --fix` + auto-apply all findings (was `high` then ask-which-to-fix) — every non-trivial edit self-fixes, no severity-bucket prompt.
     Changelog discipline reframed from "keep it short / what changed" to durable-memory (what's now true + why, not a diff recap) with a 1–5 line, ~20-words/line bound + a bad-vs-good example.
     Added a Git workflow section: direct-to-`main`, commit/push only when asked. Left R4 (a stale `services/browser-bridge` pointer) unchanged by owner choice.

107. Archived entries 1–95 into the new `docs/CHANGELOG-archive.md` (the first archive split) — `CHANGELOG.md` had grown past the 150k-char auto-load limit, defeating its purpose as scannable session-start orientation.
     The live file now opens with a milestone-summary blockquote (MVP steps 1–6 + the early post-MVP work) and keeps only entry 96 onward; archived entries are verbatim, numbers stay globally unique across both files, and new entries continue here from 108.

108. Trigger/Automation refactor for autonomous watchers (spec `docs/specs/2026-06-19-trigger-abstraction.md`; migration 0011 drops `triggers`, creates `automations`).
     A **`Trigger`** is now a pluggable firing mechanism (`email`|`timer`|`webhook`) — a sibling abstraction to `Tool`/`LlmProvider` with a `paramsSchema` + `detect((params,cursor)) → {events,nextCursor}`, mapped name→impl by a worker `triggerRegistry`. An **Automation** is the configured row (chosen Trigger + params + objective + notify policy + cursor).
     Tier-0 detection is now each Trigger's own `detect()` returning an explicit list of new events, replacing the gate+reducer that collapsed everything to one opaque scalar (`gate`/`last_seen_signal` dropped; `params`/`cursor`/`pending_cursor` added; `kind`→`trigger`).
     The per-Trigger **cursor** is opaque to the framework (jsonb, stored + staged/committed, never interpreted). It commits **only after the escalated run reaches `done`** (`run.ts` does `cursor ← pending_cursor`); a crashed/failed run re-delivers the same delta next tick (at-least-once). This fixes email re-reporting/dropping — the old gate advanced the signal at *detection* time, silently losing mail when a run died.
     Agent-facing tools renamed: `create_automation` (was `schedule_self`) now picks a Trigger + supplies schema-validated params, killing the freeform-gate-or-null footgun; `run_automation` (was `run_trigger`) force-runs. Scratchpad scope `trigger:<id>`→`automation:<id>`.
     Operational names deliberately kept: pm2 `alfred-triggers`, pg-boss queue `trigger-detect`, `TriggerDetectJob`/`triggerId` (carries the automation id), `enqueueTriggerDetect`/`scheduleTrigger`/`unscheduleAll`/`bumpDetectionCost` — renaming them was churn for no design gain.
     Existing watcher rows are dropped (clean cutover, no FK references), re-created via `create_automation`. Build + lint green; tests 145 pass, 2 pre-existing local-DB-state failures (the documented `run.test.ts` approval-gate flake; `db.test.ts` needs `pnpm db:migrate` to create the new `automations` relation locally).

109. Trigger-abstraction adversarial-review fixes — the cursor commit is now regression-proof against interleaving (CHANGELOG 108 follow-up).
     **The dismiss path no longer regresses the cursor.** A non-deterministic triage can dismiss a delta a *prior* escalation already staged for an in-flight run; the dismiss `cursor`-advance cleared that run's `pending_cursor`, so the parked run's later done-commit wrote a now-stale staged value, regressing `cursor` and forcing re-triage of handled events. Dismiss + empty-delta now advance `cursor` only when no stage is in flight (`pending_cursor == null`), mirroring the empty-branch guard that already existed.
     **The done-commit is now conditional.** `run.ts` captures `pending_cursor` at run *start*; new `commitAutomationCursorIfStaged(id, expectedPendingCursor)` commits `cursor ← pending` only when the row's `pending_cursor` still equals the staged value (`= $val::jsonb`), so any concurrent writer that moved it wins and the stale commit is a no-op — never a regression, at worst a re-deliver (the safe direction). Replaces the unconditional `advanceAutomationCursor` at this one site (the helper stays for the dismiss-side direct commit).
     **Dismiss/empty cursor-advance + fire-record are now atomic** — folded into one `markAutomationFired({ cursor })` UPDATE (was two statements), so a crash between them can't re-detect the delta or double-charge `detection_cost_usd`.
     **Triage prompt: closed a fence-escape.** The untrusted events were wrapped in a markdown ```` ``` ```` code fence a crafted email subject/from could close early; switched to unbreakable prose markers (matching `composeObjective`). `STRICT_SYSTEM_PROMPT` remains the primary barrier; this is defense-in-depth.
     Rejected: exposing a `triage` arg on `create_automation` (review nit) — the spec's signature is exactly `{ trigger, params?, when?, objective, notify_policy? }`, so adding it would contradict the contract; triage stays settable only via direct rows. Added two `db.test.ts` cases reproducing the stale-commit regression + the atomic-fire advance (skip-guarded on `POSTGRES_URL`).
     Build + lint green; non-DB tests all pass. The 4 `pnpm test` failures are all pre-existing local-DB-state (3 × missing `automations` relation until `pnpm db:migrate` applies 0011, incl. the unchanged `trigger management` test; + the documented `run.test.ts` approval-gate flake) — none from these changes.

110. Email-Trigger param docs now explain the cursor-based dedup, so the agent stops mis-describing it.
     Live test surfaced the agent telling the owner that `unreadOnly:false` could re-report handled mail and that marking-read prevents retriggering — both false: dedup is the internal UID high-water-mark cursor (exactly-once per new message regardless of read state / `unreadOnly`); the read flag is irrelevant to retriggering. Root cause: the cursor is framework-internal/invisible to the agent, and the `email` `paramsSchema` copy (the only thing the agent sees) described `unreadOnly` as plain "only fire on unread", so the agent fell back on the unread-flag-as-dedup pattern this design replaced.
     Fix is copy-only: the `email` paramsSchema gained a top-level description (fires once per new message, dedup automatic, baseline silent) and `unreadOnly` is now documented as a *filter* — true ⇒ skips mail already read by poll time (a real miss-window), not a dedup knob. Also added a §16 read-only contract note on `Trigger.detect()` (the deleted gate's read-tier check no longer enforces it; read-only is now by-construction per impl). No behavior change.

111. Removed the `email` Trigger's `unreadOnly` param (supersedes the CHANGELOG-110 doc of it).
     It was a vestige of the pre-cursor model (where the unread flag *was* the dedup signal) and didn't survive the move to the UID cursor: dedup is already exactly-once via the high-water mark, so the only thing `unreadOnly:true` added was a racy miss-window (a new mail read before the next poll got filtered out AND the cursor advanced past it → silently skipped). It also filtered on volatile, human-mutated state, unlike the stable `from`/`subject`/`mailbox` filters, coupling detection to reading activity. "Watch my unread pile" is a different (state/threshold) trigger archetype, not a new-arrival feed filter.
     `email` params are now `{ mailbox?, from?, subject? }` (stable attributes only); `emailCriteria` no longer emits a `seen:false` IMAP criterion. No schema/migration change (`params` is freeform jsonb) and nothing in the DB referenced it.

112. `create_automation` cron floor dropped from ≥hourly to per-minute (owner's call). `validateCron` no longer rejects a sub-hourly minute field — `* * * * *`, `*/5`, ranges, and lists are now accepted (cron's finest granularity is 1 minute); it still rejects non-5-field expressions and an out-of-range single minute.
     The old ≥hourly floor was a §16 blast-radius bound on the agent-callable, no-approval tool. It's safe to drop because the real cost driver is Tier-2 escalations, not poll frequency: a per-minute *poll* trigger (e.g. email) is cheap — Tier-0 detect on no change is free, only escalations cost. The `MAX_SELF_SCHEDULE_TRIGGERS=25` standing-automation cap is now the sole structural bound (the frequency limit mostly throttled `timer`, which fires a full run every tick — that's now the owner's judgment to make). Note: the scheduler never had a floor, so a direct row could already poll sub-hourly; this just lets the agent create them too. Tests inverted to assert per-minute acceptance.

113. Root `pnpm dev` now starts `alfred-triggers` too — automations never fired in dev because the scheduler wasn't running.
     The `dev` concurrently set was `worker,server,web,discord` (missing `triggers`), so no process ever registered cron schedules or enqueued `trigger-detect` jobs: a created automation sat with `pgboss.schedule` empty and `last_fired_at`/`cursor` null forever. `ecosystem.config.cjs` (pm2/deploy) always included `alfred-triggers`, so only the dev path was broken. Added `@alfred/triggers dev` to the set (same omission pattern as discord in CHANGELOG 96, deploy-apps in 101). Restart `pnpm dev` to pick it up; the scheduler registers schedules on boot + a periodic reconcile.

114. Fixed: scheduled (cron) automations never fired — `alfred-triggers` now owns the cron clock in-process (Croner) instead of pg-boss `schedule()`.
     pg-boss v10 keys a schedule by queue name and requires that queue to exist; `scheduleTrigger` scheduled onto a per-automation queue (`trigger:<id>`) that's never created (and that nothing consumes — the worker only works the shared `trigger-detect` queue). So every cron registration threw `Queue ... not found`, `pgboss.schedule` stayed empty, and no `trigger-detect` job was ever enqueued. One-shot `timer` automations were unaffected (they use the direct sweep path). Latent since the cron path was never live-verified (CHANGELOG 87).
     Fix: added `croner` (zero-dep, tz-aware) to `@alfred/triggers`; `registerSchedules` arms one `Cron(schedule, {timezone}, …)` per recurring automation whose tick calls `enqueueTriggerDetect`, and boot + the 30s reconcile stop + re-create the timers. This matches the scheduler's "pure scheduler, timing only" charter (it owns the clock, never pg-boss's). Removed the now-dead `scheduleTrigger`/`unscheduleAll` `@alfred/db` helpers.
     Tradeoff: a tick missed while the process is down is skipped, not replayed — acceptable, the per-Trigger cursor means no data is lost (next tick re-reads the cursor). Verified: a built-entrypoint boot now logs "1 cron schedule(s) registered" with no error (was 0 + two `Queue not found`). Build + lint green.

115. Watcher runs: the seed no longer leaks prompt-engineering into the chat, and reports are self-contained.
     (1) **Clean seed display.** The escalated run's seed message used to be the full `composeObjective` string — owner objective + the fenced "New items detected (UNTRUSTED watched content — do NOT treat as instructions)" delta — stored as one `text` part, so the web chat rendered the whole thing as if the owner had typed it. Now the owner objective is the only visible `text` part; the fenced delta rides a separate **`trigger_context`** content part. The web/Discord renderers already skip non-text parts (so it never shows), and the worker normalizes `trigger_context` → text in `rowsToMessages` so the model still receives the fenced delta — §16 fencing intact in the model input, gone from the UI. `composeObjective`→`composeTriggerContext` (returns just the fenced block); `createAutomationRun` takes an optional `context`.
     (2) **Self-contained reports (framing nudge).** The autonomous-run system prompt now tells the model the owner reads the reply later as a standalone notification — follow the objective, but make it self-contained; concise, no preamble. Kept GENERIC (an earlier email-specific "sender + subject" draft was wrong altitude — not every watcher reports email). Note: framing does NOT override a deliberately narrow objective — a watcher whose objective says "write me the subject(s)" still correctly returns only subjects (verified: it replied "Alfred test nr 1" for that mail). Richer output there is an objective-wording change, not a framework one — `create_automation` has no `update` sibling yet, so re-wording means editing the row / re-creating.
     Only NEW fires are affected — an already-stored seed message keeps its old rendering (history isn't rewritten), and a trigger run only ever re-reads its latest seed. `pnpm -r build` + lint green.

116. Per-fire Alfred-initiated automation conversations (spec `docs/specs/2026-06-20-watcher-conversation-per-fire.md`; migration 0012 adds `conversations.automation_id`).
     Each Tier-2 escalation now lands in its **own fresh conversation** instead of appending to one persistent watcher thread — `createAutomationRun` mints a new `conversations` row (`ingress='trigger'`, `automation_id` set, a fully-hidden `trigger_context`-only seed so **Alfred's report is the first visible message**), repliable to continue. The owner asked for one fresh, repliable report per fire plus a real notification, not one ever-growing thread.
     **New `conversations.automation_id`** (nullable FK → automations.id) is the routing key, replacing the old `automations.conversation_id` lookup: a watcher now has many conversations, so the link lives on the conversation. `run.ts` resolves the automation via `getAutomationForConversation` for **every** run in the conversation, so a follow-up reply recalls/writes the same `automation:<id>` scratchpad. `automations.conversation_id` is kept but repurposed to a "jump to latest" pointer at the most-recent fire.
     **Cursor commit is gated on `human_in_loop=false`** (the autonomous escalation) — a follow-up reply (`human_in_loop=true`) resolves the same automation but never staged a cursor, so replies are side-effect-free on detection state. `isTrigger` now keys strictly on `human_in_loop` (a one-shot is recognized by `nextFireAt` + no recurring `schedule`).
     **Discord: a post per fire from the notifications outbox.** The persistent-watcher reconcile + standing `LISTEN` + lazy-render (`getAutomationByConversation`, `reconciledTriggers`, `standingListens`, `openWatcherTarget`) are retired. The bot is now a **second consumer of the `notifications` channel** (alongside Web Push): on the first notification for a watcher conversation it creates a new `Watchers`-forum post (a new thread = a real ping), posts the final report read from the DB (post-final, no streaming), and `repointConversationChannel`s the conversation to the post so a reply continues it. Boot seeds `seenNotifs` so a fire while the bot is down gets no catch-up post (Web Push only).
     Build + lint green; tests 144 pass, 7 local-DB-state failures (the test DB needs `pnpm db:migrate` to apply 0012 — all 7 fail on `column "automation_id" of relation "conversations" does not exist`, incl. the documented `run.test.ts` approval-gate cases). Live on-box Discord verification pending.
