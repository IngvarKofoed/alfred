# Changelog

Each entry is numbered with a monotonically increasing integer. Append new entries to the end. Never reuse or reorder numbers. Numbers are globally unique across this file and any future `CHANGELOG-archive.md` — never reused.

1. Scaffolded CLAUDE.md guidance (root + services/, packages/, clients/web/, clients/ios/, chrome-extension/) and split the column-level schema out of ARCHITECTURE.md §6.1 into docs/DATABASE.md.
2. Wrote docs/specs/2026-06-01-foundation-web-ingress.md — spec for the first implementation increment (repo foundation + web ingress).
3. Corrected ARCHITECTURE.md to be OS-agnostic: dropped the Windows-first framing and all WSL2 assumptions (§3/§4/§4.1/§5/§8/§18), and simplified §12 auth to "network position is the authentication" (single user, no per-request identity).
4. Implemented the foundation + web ingress: pnpm workspace + tooling (tsconfig/eslint/prettier/vitest), packages/shared typed loadConfig(), services/webserver (Hono, /api/health, serves the SPA, loopback bind), clients/web (Vite + React + Tailwind stub showing "Alfred is reachable ✓"), and ecosystem.config.cjs. Builds, tests, lint all green.
5. Wrote docs/specs/2026-06-02-postgres-drizzle.md — spec for build-order step 2 (Postgres + Drizzle).
6. Implemented build-order step 2: packages/db with the Drizzle schema (users/conversations/messages), a drizzle-kit migration, a typed node-postgres client, and app-side UUIDv7 IDs. Added POSTGRES_URL (optional) to the shared config. Integration test round-trips user→conversation→message in a rolled-back transaction (skips without POSTGRES_URL); verified end-to-end against a local Postgres 17.
7. Wrote docs/specs/2026-06-02-agent-core-gemini.md — spec for build-order step 3 (agent core with a Gemini provider, built as a library).
8. Implemented build-order step 3 (agent core, as a library): packages/agent-core with the hand-rolled loop, the LlmProvider streaming abstraction, a GeminiProvider on @google/genai (streaming + function-calling), and the Tool interface + a built-in echo tool. Added GEMINI_API_KEY (optional) + DEFAULT_MODEL (gemini-2.5-flash) to the shared config. A fake-provider unit test covers the full tool-call round-trip offline; a live Gemini integration test + CLI runner are gated on GEMINI_API_KEY. Build/lint/offline tests green; the worker/pg-boss/NOTIFY/SSE/web pipeline is deferred to the next step.
9. Wrote docs/specs/2026-06-02-chat-pipeline.md — spec for the end-to-end text-only chat pipeline (worker + pg-boss + NOTIFY/SSE + web).
10. Implemented the chat pipeline (text-only, no tools): new alfred-worker consumes pg-boss jobs, runs the agent-core Gemini loop, persists the assistant turn, and streams tokens via Postgres NOTIFY; webserver gained POST message / history / SSE-stream routes that LISTEN and forward; added the agent_runs table (+ one-active-run-per-conversation index) and pg-boss (retryLimit 0 + long expiry) in packages/db; messages.content persists the agent-core ContentPart[]; a fixed owner user is upserted on first message (resolves the step-2 seeding deferral); the web client is now a streaming chat (hand-rolled EventSource); worker boot sweeps orphaned 'running' runs to 'failed'. Worker integration test (fake provider) verifies the worker→loop→persistence→status path against Postgres; real end-to-end chat needs GEMINI_API_KEY. ecosystem.config.cjs gained alfred-worker.
11. Fixed loadConfig (packages/shared) to load the repo-root .env regardless of cwd — it walks up to the pnpm-workspace.yaml root and loads its .env, so `pnpm --filter <pkg> dev` picks up a single root .env. Exported env vars still take precedence.
12. Documented GEMINI_API_KEY + DEFAULT_MODEL in .env.example (the LLM keys added in step 3).
13. Renamed the config key DEFAULT_MODEL -> GEMINI_MODEL (provider-scoped, so other providers can add OPENAI_MODEL/etc.). Updated shared config, the GeminiProvider, and .env.example.
14. Added dev convenience scripts (root package.json): `dev` runs worker + webserver + web together via concurrently (predev builds the libs first); `dev:up` also starts Postgres and migrates; `db:start`/`db:stop`/`db:migrate` manage local Postgres (pg_ctl paths overridable via ALFRED_PG_CTL/ALFRED_PGDATA). Added the concurrently devDependency.
15. Wrote docs/specs/2026-06-03-observability.md — spec for lightweight in-Postgres observability + a debug page (replacing Langfuse).
16. Implemented lightweight observability: a TracingProvider decorator in agent-core (wraps any provider, records each call) + a terminal `usage` StreamEvent emitted by GeminiProvider; a new llm_calls table; the worker persists a per-call trace and rolls token usage/model up onto agent_runs; webserver GET /api/debug/runs + /api/debug/runs/:id; and a /debug page in the web app (introduced react-router with Chat at / and Debug at /debug). Updated DATABASE.md (llm_calls) and ARCHITECTURE §6.5/§17. Verified: TracingProvider unit test, worker integration test (llm_calls + aggregates), and the live Gemini round-trip all pass.
17. Reconciled ARCHITECTURE.md with the code (/reconcile-docs): Gemini is now the documented default provider (OpenRouter/Anthropic/Ollama listed as swappable alternatives), Langfuse fully replaced by in-Postgres observability, the §13.1 .env example updated to GEMINI_API_KEY/GEMINI_MODEL, and §15 build order annotated with status (steps 1–4 built, "tools + approval" current, 5–6 + post-MVP planned). No code-drift bugs found — all divergences were deliberate. CONCEPT.md untouched (stack-free).
18. Wrote docs/specs/2026-06-03-tools-approval.md — spec for tools + approval (built-in tools, semantic trust-tier gating; MCP and ask_user deferred).
19. Implemented tools + approval (built via a multi-agent workflow + an integration pass). agent-core: runAgent gained requestApproval + onToolStart/onToolEnd hooks (write/destructive tiers gate before invoke; read runs freely; tool errors caught). db: tool_calls + user_interactions tables (migration 0003); pg-boss lease bumped to 4500s. worker: wires [echo, set_conversation_title] and the approval gate — creates the interaction, NOTIFYs interaction_required, blocks on LISTEN (race-safe) with a 1h timeout, resumes on resolution; persists tool_calls. webserver: GET/POST /api/interactions/:id (first-writer-wins resolve + interaction_resolved NOTIFY). web: an approval card (✅/❌) over the existing SSE. ARCHITECTURE §16/§8: per-tool tiers gate semantic tools but NOT the browser (profile isolation + domain gating + task-scoped approval at step 5); tiers are owner-assigned, never server-declared. Verified: build/lint green; full suite incl. a new approval-cycle integration test and the live Gemini round-trip all pass.
20. Web chat polish: (1) "+ New conversation" button in the header rotates the conversation id and remounts Chat clean (id lifted to App); (2) auto-scroll that pins to the bottom as messages/tokens arrive, with a stick-to-bottom guard so scrolling up to read isn't fought (instant scroll — smooth stalled under rapid token updates); (3) a refined "study at night" restyle (self-hosted Fraunces + Hanken Grotesk via @fontsource-variable — no external CDN, keeping the app on-box/offline-capable per CONCEPT; warm espresso/cream/brass theme tokens, empty-state greeting, Alfred-voiced serif bubbles, thinking dots). Verified live in-browser against the running stack; code-review (high) findings folded in.
21. Web chat readability: dropped the Fraunces serif (hard to read for the owner) and switched to clean sans throughout — Hanken Grotesk for the wordmark, greeting, Alfred message bodies, and approval card. Removed the @fontsource-variable/fraunces dependency. Verified in-browser: only Hanken Grotesk loads, all text renders sans.
22. Wired up cost accounting (previously the cost_usd column was never populated and the /debug page didn't show it). agent-core: new pricing.ts — a per-model USD price map (gemini-2.5-flash $0.30/$2.50 per 1M in/out) + computeCostUsd (unknown model → 0, never a guess). db: cost_usd added to llm_calls (migration 0004). worker: computes per-call cost at the llm_calls insert and sums it onto agent_runs.cost_usd via a shared rollupUsage helper applied on BOTH the done and failed paths — so a run that made paid calls before failing reports its true cost/tokens, not 0. webserver: /api/debug/runs now returns costUsd. web: /debug shows a Cost column and per-call cost (trailing-zero-trimmed for sub-cent legibility). DATABASE.md updated. Verified: build/lint green; new pricing unit test + full suite incl. live Gemini round-trip pass.
23. Cost accuracy: account for Gemini's implicit context caching, which bills cached prompt tokens at the cheaper cached rate. Threaded usageMetadata.cachedContentTokenCount through the GeminiProvider → the `usage` StreamEvent → LlmTrace → computeCostUsd (new 4th arg + cachedInputPerMTok $0.03 on gemini-2.5-flash; cached subset clamped to promptTokens). Without this the displayed cost was a safe over-estimate on multi-turn chats replaying history. Audio input / cache storage left as documented deferrals in pricing.ts (no audio until voice; no explicit caching). Verified: build/lint green, two new pricing tests + full suite incl. live Gemini round-trip pass.
24. Wrote docs/specs/2026-06-03-embedded-browser-bridge.md — spec for build-order step 5 (browser automation). Chosen approach: **Option C** — embed the WebSocket bridge in alfred-worker (no separate process, no MCP), expose browser commands as built-in tools, port the extension + bridge from the owner's chrome-mcp. Settled: multi-tab model; loopback bind + chrome-extension:// Origin guard instead of an auth token; every browser tool `write`-tier to start; screenshots/vision deferred to a follow-up.
25. Implemented step 5 (embedded browser bridge, Option C). worker: services/worker/src/browser/ — bridge.ts (WebSocket server lifted from chrome-mcp, bound to 127.0.0.1 with a chrome-extension:// Origin guard, uses crypto.randomUUID; process-wide singleton) + tools.ts (makeBrowserTools → 22 built-in Tools, all `write`-tier, invoke → bridge.sendCommand; 100k-char cap on string results). run.ts spreads the browser tools into the run's tool array; index.ts starts the bridge at boot and stops it on SIGINT/SIGTERM. shared: added BRIDGE_WS_PORT (default 7865). chrome-extension/: new pnpm workspace member ported from chrome-mcp (esbuild build), WS_URL → ws://127.0.0.1:7865, rebranded; screenshot handlers kept but no screenshot tools exposed. Docs reconciled: ARCHITECTURE §2/§5/§7.3/§7.6/§8/§13.1/§14.3/§15/§16/§17/§18, .env.example, services/ + chrome-extension/ CLAUDE.md. Post-review hardening (code-review high): the bridge is best-effort on a listen error (wss.on('error') — a taken port no longer crashes the worker / kills agent-run consumption, e.g. pm2-reload overlap or a second worker §7.4); in-flight commands are drained (rejected fast) on extension disconnect/reconnect instead of waiting out the 30s timeout; the process-static browser toolset is built once at module load, not per run; and the 100k result cap now also covers large structured results (e.g. get_links), not just bare strings. Verified: full build + lint green; all 13 tests pass; a runtime harness confirmed the Origin guard (accept chrome-extension://, reject https://), best-effort port-in-use, and fast pending-command drain. Extension built by esbuild — not tsc-typechecked in CI, matching upstream (latent upstream type annotations tolerated). Live browser behavior to be confirmed with `verify` in a real Chrome (MV3 lifecycle isn't unit-testable).
26. Debug-page observability: show the *full* LLM exchange, not just messages+text. agent-core: TracingProvider now also captures the `tools` offered (name/description/parameters) and the `responseToolCalls` the model returned (id/name/args) on each LlmTrace. db: llm_calls gains `tools` + `response_tool_calls` jsonb (migration 0005). worker persists both. webserver: /api/debug/runs/:id now also returns the run's tool_calls rows (name/args/result/trust_tier/status). web /debug: each LLM call shows the system instruction (extracted from the request), tools offered (N), request messages, and a response panel that now renders the model's tool calls (so a tool-only turn is no longer blank); plus a per-run "Tool calls" section with results + approval outcomes. Closes the gap where tools/system/tool-calls were sent/recorded but invisible. DATABASE.md updated. Post-review (code-review high): guarded detail.toolCalls with `?? []` against a stale API shape, and extended the TracingProvider unit test to assert the captured tools + responseToolCalls. Verified: build/lint green; all 13 tests pass — the TracingProvider unit test asserts the new fields and the approval-cycle integration test confirms tools (incl. browser tools) + the model's tool call land in llm_calls. Live /debug rendering to confirm in-browser with `verify`.

